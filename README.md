# DBClient releases

Signed and notarized builds of DBClient, a native macOS database client.

## Install

```sh
brew tap az-code-lab/taps
brew install --cask dbclient
```

Apple Silicon, macOS 15 or newer. Or download the `.dmg` from the
[latest release](../../releases/latest).

This repository holds released builds only — the source is private.
